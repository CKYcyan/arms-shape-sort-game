# ARMS 展覽互動 — 海洋生物分類小遊戲 架構文件

> 最後更新：2026-03-13（v3 SVG icon 版完成）

---

## 一、核心玩法與邏輯設定

### 遊戲概念
面向一般大眾的展場互動小遊戲。玩家將畫面上的海洋生物圖卡，拖曳到對應的分類框中，完成後送出答案，查看得分與解說。

### 遊戲流程（三個畫面）

```
開場畫面（screen-start）
  ↓ 按「開始遊戲」
遊戲畫面（screen-game）
  ↓ 全部生物分類完畢，按「送出答案」
結果畫面（screen-result）
  ↓ 按「再玩一次」回到開場
```

### 核心機制
- **生物清單（SHAPES）**：每個生物有 `id`、`name`（中文名）、`category`（所屬分類 id）、`svg` 或圖片來源
- **分類清單（CATEGORIES）**：每個分類有 `id`、`label`（中文標籤）、`color`（代表色）
- **洗牌**：每次開始遊戲，生物順序隨機打亂
- **拖曳系統**：自製統一方案，同時支援滑鼠與觸控（展場 tablet 友善）
- **放置邏輯**：生物可重複拖移、可換框；`placement` 物件記錄 `{ shapeId: categoryId }`
- **提交判斷**：所有生物都放入框後才啟用送出按鈕；送出後逐一比對 `placement[id]` 與 `shape.category`
- **結果顯示**：顯示總分、每題對錯、正確答案

### 計畫中的海洋生物版本調整
- 將 SVG 形狀替換為海洋生物圖片（`.png` 透明背景）
- 分類改為海洋生物學分類（預計 4～6 類，如：魚類、哺乳類、甲殼類、頭足類、棘皮動物等）
- 圖片統一存放於 `gallery/images/`
- 可加入生物簡介文字（結果畫面顯示）

---

## 二、檔案目錄結構

```
GAME/
├── index.html              ← 首頁導覽（連到各互動區入口）
├── shape_sort_game.html   ← 【主要遊戲】形狀分類原型（海洋生物版本的基礎）
├── architecture.md        ← 本文件：架構說明、開發紀錄
├── README.md              ← 簡易說明（資料夾結構、連結寫法、GitHub Pages 網址）
├── .gitignore
└── gallery/
    ├── index.html          ← 生物圖鑑入口（目前為佔位頁，待開發）
    └── images/             ← 圖鑑與遊戲用圖片統一放這裡
        └── .gitkeep
```

### 各檔案職責

| 檔案 | 職責 |
|------|------|
| `index.html` | 首頁，導覽到遊戲與圖鑑 |
| `shape_sort_game.html` | 拖曳分類遊戲主體，含 HTML / CSS / JS 全部邏輯 |
| `gallery/index.html` | 生物圖鑑首頁（待填充內容） |
| `gallery/images/` | 所有生物圖片資源（命名規則：英文小寫 + 底線，如 `clownfish.png`） |
| `architecture.md` | 開發脈絡、架構說明、To-Do 紀錄（本文件） |
| `README.md` | 對外說明文件，含 GitHub Pages 網址 |

---

## 三、開發進度

### 已完成
- [x] 首頁（`index.html`）建立，深色主題，導覽連結正常
- [x] 形狀分類原型（`shape_sort_game.html`）完整可運作（v2，保留備查）
- [x] 資料夾結構規劃完畢（`gallery/images/` 已建立）
- [x] GitHub Pages 部署設定（`https://CKYcyan.github.io/arms-shape-sort-game/`）
- [x] 架構文件（本文件）建立
- [x] **v3 海洋生物版（`marine_sort_game.html`）完成**
  - 4 大分類：魚類 / 甲殼類 / 棘皮動物 / 軟體動物
  - 12 種生物，每類各 3 種，全部使用手工 SVG icon
  - 每種生物附中文簡介，顯示於結果畫面
  - 海洋深藍主題視覺
  - 結果畫面顯示 icon + 名稱 + 說明 + 對錯標示

### 生物清單（v3 SVG 版）
| 分類 | 生物 | id |
|------|-----|----|
| 魚類 | 小丑魚、鯊魚、海馬 | s1~s3 |
| 甲殼類 | 螃蟹、蝦子、龍蝦 | s4~s6 |
| 棘皮動物 | 海星、海膽、海參 | s7~s9 |
| 軟體動物 | 章魚、烏賊、蛤蜊 | s10~s12 |

### 進行中
- [ ] 實機測試 v3（觸控裝置 / 各瀏覽器）
- [ ] 視需求收集海洋生物真實圖片，替換 SVG icon

### 待辦事項（To-Do List）

#### 高優先
- [ ] 觸控裝置測試（展場 tablet）
- [ ] 評估是否用真實圖片取代 SVG（需提供 PNG 圖片與對照表）

#### 中優先
- [ ] 開發 `gallery/index.html` 生物圖鑑頁面
- [ ] 考慮在結果畫面加入每種生物的簡短介紹
- [ ] 手機版面適配確認（小螢幕分類框排版）

#### 低優先 / 未來擴充
- [ ] 多關卡設計（難度遞增）
- [ ] 音效回饋
- [ ] 計時模式

---

## 四、已知問題

| 問題 | 狀態 |
|------|------|
| 拖曳時 `drag-over` 視覺效果依賴 `mouseover`，在觸控裝置無作用 | 已知，低優先，不影響功能 |
| 目前 `gallery/index.html` 為空殼佔位頁 | 待開發 |

---

## 五、內容準備規範（給素材提供者）

要新增海洋生物，請提供：

1. **圖片**：放入 `gallery/images/`，格式 PNG，正方形，建議 200×200px，透明背景
2. **命名**：英文小寫 + 底線，例如 `blue_whale.png`
3. **對照表**（告知開發者即可）：

```
生物中文名 | 圖片檔名          | 分類
---------|-----------------|------
小丑魚    | clownfish.png   | 魚類
藍鯨      | blue_whale.png  | 哺乳類
螃蟹      | crab.png        | 甲殼類
```
